/**
 * @file main.cpp
 * @brief Classe Aluno
 *
 * Crie uma classe Aluno em C++ com atributos privados nome (string),
 * matricula (string) e tres notas (array de 3 doubles). Implemente
 * metodos publicos para: definir os dados do aluno, calcular a media
 * das notas, verificar se o aluno esta aprovado (media >= 7.0) e
 * exibir um boletim com todas as informacoes. No programa principal,
 * crie dois objetos Aluno, defina seus dados e exiba os boletins
 * de ambos.
 */

#include <iostream>
#include <string>
using namespace std;

int main() {
    return 0;
}
