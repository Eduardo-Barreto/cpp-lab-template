/**
 * @file main.cpp
 * @brief Classe Pessoa
 *
 * Crie uma classe Pessoa em C++ com atributos privados nome (string) e
 * idade (int). Implemente metodos publicos para definir e obter esses
 * valores. No programa principal, crie dois objetos do tipo Pessoa,
 * defina seus dados e imprima as informacoes de ambos.
 */

#include <iostream>
#include <string>
using namespace std;

int main() {
    return 0;
}
