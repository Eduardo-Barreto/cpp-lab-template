/**
 * @file main.cpp
 * @brief Classe ContaBancaria
 *
 * Crie uma classe ContaBancaria em C++ com atributos privados titular
 * (string), saldo (double) e numeroConta (int). Implemente metodos
 * para: depositar um valor, sacar um valor (apenas se houver saldo
 * suficiente), consultar o saldo atual e exibir os dados da conta.
 * No programa principal, crie uma conta, realize operacoes de deposito
 * e saque, e exiba o saldo final.
 */

#include <iostream>
#include <string>
using namespace std;

int main() {
    return 0;
}
